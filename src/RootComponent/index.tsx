import React from 'react';
import IRouter from 'Router';

const RootComponent: React.FC = () => {

    return (
        <article className='RootComponent'>
            <IRouter />
        </article>
    );
};

export default RootComponent;
